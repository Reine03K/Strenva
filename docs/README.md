# Project Documentation

This `docs/` directory provides a file-by-file overview for both the backend (Node.js/Express/Mongoose) and the frontend (React). Use these docs to understand structure, responsibilities, and key integration points.

- See `backend/` for API structure, models, middleware, and routes.
- See `frontend/` for React app architecture, views, components, and API usage.


